@inherits LayoutComponentBase
@inject IBootstrapCSS BootstrapCSS

<Navbar/>
<LanguagePicker/>

<BSContainer>
    <BSRow>
        <BSCol XS="1"></BSCol>
        <BSCol XS="10">@Body</BSCol>
        <BSCol XS="1"><VerticalLinks /></BSCol>
    </BSRow>
</BSContainer>

<div style="min-height: 2000px"></div>
<Footer/>


@code {
    protected override async Task OnInitializedAsync()
    {
        await BootstrapCSS.SetBootstrapCSS("4.3.1");
    }
}